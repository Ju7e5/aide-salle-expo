[build]
builder = "nixpacks"
buildCommand = "npx prisma generate && npx prisma db push && npm run db:seed && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
